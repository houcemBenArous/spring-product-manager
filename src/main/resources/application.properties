spring.application.name=gestioncatalogue1111

server.port=8081

# Updated to use default MySQL port 3306 and empty password (default XAMPP config)
spring.datasource.url=jdbc:mysql://localhost:3306/catalogue2026?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root123*

### JPA / HIBERNATE ###
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
# Updated to use MySQL8Dialect for better compatibility
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect